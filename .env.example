# NEVER commit a real .env
HL_ACCOUNT_ADDRESS=0xYourMainOrSubaccountAddress

# API wallet private key (agent wallet) used to sign exchange actions
HL_API_WALLET_PRIVATE_KEY=0xYourApiWalletPrivateKey

# Optional: override endpoints (leave blank for defaults)
HL_API_URL=